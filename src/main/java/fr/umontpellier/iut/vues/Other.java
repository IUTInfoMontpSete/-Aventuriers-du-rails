package fr.umontpellier.iut.vues;

public class Other {

    /*
    public VueJoueurCourant(ObservableList<IJoueur> ObservJoueur) {
        VueJoueurCourant.ObservJoueur = ObservJoueur;
        VueJoueurCourant.joueurChangement = (ListChangeListener.Change<? extends IJoueur> c) -> {
            while (c.next()) {
                if (c.wasAdded()) {
                    for (IJoueur joueur : c.getAddedSubList()) {
                        VueJoueurCourant.this.ijoueur = joueur;
                        VueJoueurCourant.this.setStyle("-fx-background-color: " + joueur.getCouleur());
                        VueJoueurCourant.this.setOnMouseClicked(event -> {
                            VueJoueurCourant.this.setStyle("-fx-background-color: " + joueur.getCouleur());
                        });
                    }
                }
            }
        };
        VueJoueurCourant.ObservJoueur.addListener(VueJoueurCourant.joueurChangement);
    }
     */

    /*
    private static ObservableList<IJoueur> ObservJoueur; // Valeur actuelle
    private static ListChangeListener<IJoueur> joueurChangement; // Valeur apres changement
     */
}
